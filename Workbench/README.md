# Workbench

Use this directory as the single workspace for new code, exercises, notes, and project files.

When a set of files is ready, ask for an analysis of `Workbench`. The contents can then be grouped by topic, moved into a permanent category, and the temporary source directory removed after the move is verified.

Keep unrelated experiments in separate subdirectories to make later classification straightforward.
